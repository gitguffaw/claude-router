{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/gitguffaw/claude-router/schemas/models-output.schema.json",
  "title": "Claude Router Models Catalog",
  "type": "object",
  "required": ["catalog_version", "discovery", "models", "cli_fields", "tiers", "effort_levels", "modifiers", "permission_modes", "presets", "lean_profiles"],
  "additionalProperties": false,
  "properties": {
    "catalog_version": {
      "type": "string",
      "description": "Semver of the catalog data."
    },
    "discovery": {
      "$ref": "#/$defs/ModelDiscovery"
    },
    "models": {
      "type": "array",
      "items": { "$ref": "#/$defs/ModelOption" }
    },
    "cli_fields": {
      "type": "array",
      "items": { "$ref": "#/$defs/CliField" }
    },
    "tiers": {
      "type": "array",
      "items": { "$ref": "#/$defs/ModelTier" }
    },
    "effort_levels": {
      "type": "array",
      "items": { "$ref": "#/$defs/EffortLevel" }
    },
    "modifiers": {
      "type": "array",
      "items": { "$ref": "#/$defs/Modifier" }
    },
    "permission_modes": {
      "type": "array",
      "items": { "$ref": "#/$defs/PermissionMode" }
    },
    "presets": {
      "type": "array",
      "items": { "$ref": "#/$defs/Preset" }
    },
    "lean_profiles": {
      "type": "array",
      "items": { "$ref": "#/$defs/LeanProfile" }
    }
  },
  "$defs": {
    "ModelDiscovery": {
      "type": "object",
      "required": ["status", "source", "claude_version", "selectors", "aliases", "full_names", "error", "completeness", "notes"],
      "additionalProperties": false,
      "properties": {
        "status": { "type": "string", "enum": ["available", "no-model-data", "not-run", "unavailable"] },
        "source": { "type": "string" },
        "claude_version": { "type": ["string", "null"] },
        "selectors": {
          "type": "array",
          "items": { "type": "string" }
        },
        "aliases": {
          "type": "array",
          "items": { "type": "string" }
        },
        "full_names": {
          "type": "array",
          "items": { "type": "string" }
        },
        "error": { "type": ["string", "null"] },
        "completeness": { "type": "string", "enum": ["documented-examples"] },
        "notes": { "type": "string" }
      }
    },
    "ModelOption": {
      "type": "object",
      "required": ["id", "selector", "display_name", "full_name", "aliases", "source", "tier", "notes"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "selector": { "type": "string" },
        "display_name": { "type": "string" },
        "full_name": { "type": ["string", "null"] },
        "aliases": {
          "type": "array",
          "items": { "type": "string" }
        },
        "source": { "type": "string", "enum": ["claude-help"] },
        "tier": { "type": ["string", "null"] },
        "notes": { "type": "string" }
      }
    },
    "CliField": {
      "type": "object",
      "required": ["flag", "option", "optionAliases", "inputKeys", "kind", "repeatable", "valueHint", "choices", "description", "sensitive", "dangerous", "source"],
      "additionalProperties": false,
      "properties": {
        "flag": { "type": "string" },
        "option": { "type": "string" },
        "optionAliases": { "type": "array", "items": { "type": "string" } },
        "inputKeys": { "type": "array", "items": { "type": "string" } },
        "kind": { "type": "string", "enum": ["boolean", "value", "optional-value"] },
        "repeatable": { "type": "boolean" },
        "valueHint": { "type": ["string", "null"] },
        "choices": { "type": "array", "items": { "type": "string" } },
        "description": { "type": "string" },
        "sensitive": { "type": "boolean" },
        "dangerous": { "type": "boolean" },
        "source": { "type": "string", "enum": ["claude-help"] }
      }
    },
    "ModelTier": {
      "type": "object",
      "required": ["id", "flag", "display_name", "context_window", "long_context_window", "supports_ultrathink", "supports_long_context", "cost_tier", "notes"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "flag": { "type": "string", "description": "CLI flag name, e.g. --haiku" },
        "display_name": { "type": "string" },
        "context_window": { "type": "string", "description": "Human-readable base context size" },
        "long_context_window": { "type": ["string", "null"], "description": "Context size with --long-context, null if unsupported" },
        "supports_ultrathink": { "type": "boolean" },
        "supports_long_context": { "type": "boolean" },
        "cost_tier": { "type": "string", "enum": ["low", "medium", "high"] },
        "notes": { "type": "string" }
      }
    },
    "EffortLevel": {
      "type": "object",
      "required": ["id", "flag_value", "description", "token_budget", "recommended_for", "source"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "flag_value": { "type": "string", "description": "Value passed to --effort" },
        "description": { "type": "string" },
        "token_budget": { "type": ["string", "null"], "description": "Relative token budget hint when the live CLI publishes one" },
        "recommended_for": { "type": ["string", "null"] },
        "source": { "type": "string", "enum": ["claude-help"] }
      }
    },
    "Modifier": {
      "type": "object",
      "required": ["id", "flag", "type", "description", "compatible_tiers", "conflicts_with", "notes"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "flag": { "type": "string" },
        "type": { "type": "string", "enum": ["boolean", "value"] },
        "description": { "type": "string" },
        "compatible_tiers": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Tier ids this modifier works with. Empty array means all."
        },
        "conflicts_with": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Modifier ids that cannot be combined with this one."
        },
        "notes": { "type": "string" }
      }
    },
    "PermissionMode": {
      "type": "object",
      "required": ["id", "flag_value", "description", "requires_allow_dangerous", "notes", "source"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "flag_value": { "type": "string", "description": "Value passed to --permission-mode" },
        "description": { "type": "string" },
        "requires_allow_dangerous": { "type": "boolean", "description": "Whether this mode requires --allow-dangerous to activate" },
        "notes": { "type": "string" },
        "source": { "type": "string", "enum": ["claude-help"] }
      }
    },
    "Preset": {
      "type": "object",
      "required": ["id", "flag", "resolves_to", "description"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string" },
        "flag": { "type": "string" },
        "resolves_to": {
          "type": "object",
          "required": ["tier", "effort", "modifiers"],
          "additionalProperties": false,
          "properties": {
            "tier": { "type": "string" },
            "effort": { "type": ["string", "null"] },
            "modifiers": { "type": "array", "items": { "type": "string" } }
          }
        },
        "description": { "type": "string" }
      }
    },
    "LeanProfile": {
      "type": "object",
      "required": ["id", "router_value", "isolation_flag", "auth_path", "available", "description"],
      "additionalProperties": false,
      "properties": {
        "id": { "type": "string", "enum": ["auto", "oauth", "api"] },
        "router_value": { "type": "string", "enum": ["auto", "oauth", "api"] },
        "isolation_flag": { "type": ["string", "null"] },
        "auth_path": { "type": "string" },
        "available": { "type": "boolean" },
        "description": { "type": "string" }
      }
    }
  }
}
