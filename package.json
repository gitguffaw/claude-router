{
  "name": "@compound/claude-router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Codex plugin runtime for policy-backed Claude CLI delegation.",
  "license": "Apache-2.0",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate:plugin": "node scripts/validate-plugin.mjs"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
