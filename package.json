{
  "name": "@compound/claude-router-marketplace",
  "version": "2.2.0",
  "private": true,
  "type": "module",
  "description": "Claude Router marketplace for policy-backed Claude CLI delegation across Codex, Claude Code, and AGY.",
  "license": "Apache-2.0",
  "scripts": {
    "test": "cd plugins/claude-router && node --test tests/*.test.mjs",
    "validate:plugin": "cd plugins/claude-router && node scripts/validate-plugin.mjs",
    "validate:manifests": "node scripts/validate-manifests.mjs",
    "validate": "npm run validate:plugin && npm run validate:manifests"
  },
  "engines": {
    "node": ">=18.18.0"
  }
}
